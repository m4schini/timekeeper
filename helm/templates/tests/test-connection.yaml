apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "timekeeper.fullname" . }}-test-connection"
  labels:
    {{- include "timekeeper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "timekeeper.fullname" . }}:{{ .Values.service.http.port }}']
  restartPolicy: Never
